apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-credentials
  namespace: snooker-livescore
type: Opaque
stringData:
  username: "admin"
  password: "admin123"

# NOTE: Replace the username/password above with the actual RabbitMQ credentials
# if different. Apply with:
# kubectl apply -f k8s/rabbitmq-credentials.yaml -n snooker-livescore
